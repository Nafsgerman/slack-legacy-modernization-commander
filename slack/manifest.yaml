display_information:
  name: Legacy Modernization Commander
features:
  bot_user:
    display_name: Legacy Commander
    always_online: false
  slash_commands:
    - command: /legacy
      description: Run a legacy modernization assessment
      usage_hint: assess claims-batch
      should_escape: false
oauth_config:
  scopes:
    bot:
      - commands
      - chat:write
settings:
  socket_mode_enabled: true
  interactivity:
    is_enabled: true
settings:
  socket_mode_enabled: true
  interactivity:
    is_enabled: true
  event_subscriptions:
    bot_events:
      - app_home_opened
